import React, {Component} from "react";
import '../styling/style.css';
import {
  Form, Button, Container
} from 'react-bootstrap';
import ReactDOM from 'react-dom';

const AddRecipe = () => {
  return (
    <div>
      we in the add recipe list your
    </div>
  )
}

export default AddRecipe;